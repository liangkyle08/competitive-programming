#include <bits/stdc++.h>
using namespace std;

#define fi first
#define se second

const int MOD = 1e9+7;
const int INF = (1<<30);
const int ALP = 26;
const long long LL_INF = (1LL<<60);

int T, N;

void solve() {
    cin >> N;
    vector<bool> seen(N + 5);
    vector<pair<int, int>> edges;
    queue<int> q;
    q.push(1);
    while (!q.empty()) {
        cin >>
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cin >> T;
    while (T--) {
        solve();
    }
}
